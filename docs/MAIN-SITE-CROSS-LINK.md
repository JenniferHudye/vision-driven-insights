# Cross-Linking Instructions for visiondrivenglobal.com

Forward this to whoever manages Jennifer's main site (visiondrivenglobal.com). Three small things make the two sites read as one person to AI and to Google.

1. **Add a nav and footer link to this site** (insights.visiondrivenglobal.com, or the final domain once chosen). Label it "Articles" or "Insights."

2. **Match the identity block character for character.** On both sites, Jennifer's full name, business name, and one-sentence identity line should read identically:

   - Name: `Jennifer Hudye`
   - Business: `Vision Driven`
   - Identity line: `Jennifer Hudye is a vision and messaging expert who helps 7- and 8-figure entrepreneurs and CEOs clarify a 3-year Vivid Vision for their business and life, then turn it into a 90-day plan they will actually follow.`

   An exact match (not a paraphrase) is how AI systems connect the two sites as belonging to the same person.

3. **Add the DNS record for this site's subdomain**, if it lives on the same domain (e.g. `insights.visiondrivenglobal.com`). This site's builder will provide the exact record (a CNAME pointing at Vercel) when the domain step runs.

That's the whole list. No code changes beyond a nav link and a DNS record.
